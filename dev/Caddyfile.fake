# Заглушка для совместимости с интерфейсом botguard. С reload_command: "true"
# в dev-конфиге этот файл не читается — но путь до него всё равно валидируется
# на старте, поэтому пусть будет.
{
    admin off
}
